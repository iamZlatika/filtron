generator client {
  provider = "prisma-client-js"
  output   = "../prisma/generated"
}

datasource db {
  provider = "postgresql"
}

model News {
  id        String   @id @default(cuid())
  title_uk  String
  text_uk   String   @db.Text
  title_ru  String
  text_ru   String   @db.Text
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model User {
  id       String @id @default(cuid())
  login    String @unique
  password String
}

model LoginAttempt {
  id          String   @id @default(cuid())
  ip          String   @unique
  count       Int      @default(1)
  lastAttempt DateTime @default(now())
}
